<%= form_for(@question_downvote) do |f| %>
  <% if @question_downvote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question_downvote.errors.count, "error") %> prohibited this question_downvote from being saved:</h2>

      <ul>
      <% @question_downvote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
