<%= form_for(@answer_upvote) do |f| %>
  <% if @answer_upvote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@answer_upvote.errors.count, "error") %> prohibited this answer_upvote from being saved:</h2>

      <ul>
      <% @answer_upvote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
