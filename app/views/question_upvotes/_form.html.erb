<%= form_for(@question_upvote) do |f| %>
  <% if @question_upvote.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@question_upvote.errors.count, "error") %> prohibited this question_upvote from being saved:</h2>

      <ul>
      <% @question_upvote.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
